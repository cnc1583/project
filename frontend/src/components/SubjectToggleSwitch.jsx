/*
import React from "react";
import "./SubjectToggleSwitch.css"

function SubjectToggleSwitch({graphType, onToggle}) {
    return (
        <div className="subject-toggle-switch">
            <input
                type="checkbox"
                id="subject-toggle"
                className="subject-toggle-input"
                checked={graphType === "real_estate"}
                onChange={(e) => onToggle(e.target.checked ? "real_estate" : "stock")}
            />
            <label htmlFor="subject-toggle" className="subject-toggle-label">
                <span className="subject-toggle-inner"/>
                <span className="subject-toggle-switcher"/>
            </label>
        </div>
    );
}

export default SubjectToggleSwitch;
*/